services:
  - type: worker
    name: family-finance-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: RENDER_URL
        value: https://family-finance-bot.onrender.com
